You are writing section {{ position }} of a SINGLE, continuous blog post titled "{{ title }}".

Full outline (context; current section marked **bold**):
{{ outline_md }}
{% if story_so_far %}
Earlier sections that have already been written (context):
{{ story_so_far }}
{% endif %}{% if whats_next %}
Later-section briefs (context):
{{ whats_next }}
{% endif %}
Current section title (context): {{ section_title }}
Current section brief (context): {{ section_brief }}
Target length (context): ~{{ target_section_words }} words.
{% if is_first %}
Opening-hook context (already shown to the reader): {{ opening_hook }}
{% endif %}

Rule: Write flowing Markdown prose.
Because: The generated body must read naturally inside the article.

Rule: Do not include the section title as a heading.
Because: The renderer adds the heading around this independently generated section.

Rule: Treat this section as one movement in a continuous argument, not a self-contained article.
Because: The reader is moving through one post and should not feel the argument restart.

Rule: Do not re-introduce the topic, restate earlier points, or reuse earlier phrasing, metaphors, or examples.
Because: The reader has already read the earlier sections, so repetition breaks continuity.

Rule: Do not cover the ground reserved for later sections.
Because: Future sections need their own distinct contribution to the argument.

Rule: Vary how this section opens and use a natural handoff from the previous section toward the next.
Because: Fresh transitions make separate generation calls read like one continuous draft.
{% if is_first %}
Rule: Establish the conflict, question, or surprise without repeating or paraphrasing the opening hook.
Because: The hook has already opened the post, and this section must pick up where it leaves off.
{% elif is_last %}
Rule: Land the argument using the closing beat in the brief instead of mechanically summarizing earlier sections.
Because: A closing should resolve the throughline rather than repeat the article.
{% else %}
Rule: Pick up the thread from the previous section and hand off toward the next one.
Because: Middle sections must advance the argument between the surrounding movements.
{% endif %}
Rule: Use the author's voice.
Because: This section must sound like part of the same authored post.

Rule: Never use banished words or phrases.
Because: Those terms conflict with the author's established voice and explicit preferences.

Rule: Do not copy the `Rule` or `Because` labels or their rationales into the section.
Because: The response must contain clean article prose for streaming into the editor.
