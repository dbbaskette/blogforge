You are writing section {{ position }} of a SINGLE, continuous blog post titled "{{ title }}". The outline is a content guide, not a list of standalone essays — this section is one movement in one coherent argument and must read as part of the same piece, not a self-contained article.

Full outline (current section marked **bold**):
{{ outline_md }}
{% if story_so_far %}
What has already been written (earlier sections of THIS post). Continue from here — do NOT re-introduce the topic, restate points already made, or reuse their phrasing, metaphors, or examples:
{{ story_so_far }}
{% endif %}{% if whats_next %}
Sections that come AFTER this one — do NOT cover this ground; leave it for them:
{{ whats_next }}
{% endif %}
This section's title: {{ section_title }}
This section's brief: {{ section_brief }}
Target length: ~{{ target_section_words }} words.

Write the section as flowing markdown prose. Do NOT include the section title as a heading; the renderer adds it.
{% if is_first %}This is the OPENING section — establish the conflict/question/surprise. The post already opens with a hook ("{{ opening_hook }}"); do NOT repeat or paraphrase it — pick up where it leaves off.
{% elif is_last %}This is the CLOSING section — land the argument; the brief above tells you the closing beat. Build on what came before instead of summarizing it mechanically.
{% else %}Pick up the thread from the previous section and hand off toward the next. Open mid-thought with a fresh transition — do NOT start the way earlier sections start.
{% endif %}
Continuity rules: this is ONE article written straight through. Don't recycle stock phrases, metaphors, or examples that earlier sections already used — if an image or analogy appeared above, reach for a different one. Vary how each section opens. Assume the reader has just read everything above; write the next part, not a restart.
Use the author's voice. Banished words/phrases never appear.
